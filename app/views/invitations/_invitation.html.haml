.container.profile
  - @invitations.each do |invitation|
    %p[invitation]
    .container-fluid.well.span6
      .row-fluid
        .span2
          %img.img-circle{:src => "#{invitation.user.avatar}" , :alt => "#{invitation.user.avatar}"}/
        .span8
          %h3 "#{invitation.user.name}"
          %h6 Email: "#{invitation.user.email}"
          %h6 Location: "#{invitation.user.location}"
          %h6= link_to "Accept" , "#" , class: "btn btn-primary"
           