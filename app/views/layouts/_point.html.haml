#fitness_points
  - if user_signed_in? 
    %a.nav-link{ :href => "#"}
      Fitness Points 
      %span.badge.badge-default #{@user_calculated_points}

  - else 
    %a.nav-link{ :href => "#"}
      Fitness Points 
      %span.badge.badge-default 0
